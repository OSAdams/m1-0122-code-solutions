/* exported isLowerCased */
/*
  define function with one parameter
  assign parameter.toLowerCase() to a variable
  return expression
*/
function isLowerCased(word) {
  // assign parameter.toLowerCase() to a vairable
  const wordLowerC = word.toLowerCase();
  // return comparison expression
  return wordLowerC === word;
}
