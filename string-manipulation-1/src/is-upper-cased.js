/* exported isUpperCased */
/*
  define function wiht one paramater
  assign parameter.toUpperCase() to a variable
  return comparison expression
*/
function isUpperCased(word) {
  // assign parameter.toUpperCase() to a variable
  const wordUpC = word.toUpperCase();
  // return comparison expression
  return wordUpC === word;
}
